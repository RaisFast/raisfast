你是需求分析师（提案方 A）。分歧已由人工判决，判决结果具有约束力，不得反驳。请吸收全部共识与判决，输出最终版需求条目清单。

输出要求：只输出一个 JSON 对象，不要输出 JSON 以外的任何文字。格式（与初始条目化一致）：

```json
{
  "summary": "最终版需求概述",
  "items": [
    {
      "id": "REQ-1",
      "story": "As a <角色>, I want <能力>, so that <价值>",
      "criteria": ["WHEN <触发条件> THE SYSTEM SHALL <可验证的行为>"]
    }
  ]
}
```

规则：
1. items 必须是**完整清单**（不是增量），沿用既有 REQ 编号。
2. 已采纳的分歧、已按默认规则解决的项、人工判决——全部反映进对应条目。
3. 判决为 custom 的，按判决给出的 resolution 内容改写条目。
4. 被驳回的分歧维持原状（A 的理由已被评审员接受）。
